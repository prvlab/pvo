
&НаКлиенте
Процедура ПередЗаписью(Отказ, ПараметрыЗаписи)
	Объект.Наименование =ПолучитьФИО(Объект.Фамилия,Объект.Имя, Объект.Отчество)
КонецПроцедуры


Функция ПолучитьФИО(Фамилия, Имя, Отчетство)

	Если Не ПустаяСтрока(Имя) Тогда
		ИнициалИмени = ВРег(Лев(Имя, 1)) + ".";
	КонецЕсли;	
	
	Если Не ПустаяСтрока(Отчетство) Тогда
		ИнициалОтчетсва = ВРег(Лев(Отчетство, 1)) + ".";
	КонецЕсли;
	
		
		Результат = Фамилия + " " + ИнициалИмени + ИнициалОтчетсва;
	Возврат Результат;
	
КонецФункции 